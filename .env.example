# Example env file for running the Express chat server
# Currently no secrets are required for the mock server.
# If you later add SMTP or AI keys, put them here.

# Port to run the local Express server (default 5174)
PORT=5174

# Example: if you later add OpenAI or SMTP credentials
# OPENAI_API_KEY=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
